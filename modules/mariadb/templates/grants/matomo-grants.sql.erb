-- Grants for 'matomo'@''

GRANT CREATE, PROCESS
    ON *.* TO 'matomo'@'%'
    IDENTIFIED BY '<%= @piwik_password %>'
    REQUIRE SSL;

GRANT SELECT, INSERT, UPDATE, INDEX, DELETE, CREATE, DROP, ALTER, CREATE TEMPORARY TABLES, LOCK TABLES
    ON `matomo`.* TO 'matomo'@'%';
